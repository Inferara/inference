struct Point {
    x: i32;
    y: i32;
}

spec AggQuant {
    fn arr() forall {
        let a: [i32; 3] = @;
        assert(a[0] == a[0]);
    }
    fn strct() forall {
        let p: Point = @;
        assert(p.x == p.x);
    }
}
