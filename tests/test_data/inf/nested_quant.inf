fn precond(x: i32) -> i32 {
    return x;
}

spec NestedQuant {
    fn demo(x: i32) -> () forall {
        assume {
            precond(x);
        }
        exists {
            let y: i32 = @;
            assert(y == y);
        }
    }
}
