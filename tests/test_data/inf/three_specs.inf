pub fn main() -> i32 {
  return 0;
}

fn one() -> i32 {
  return 1;
}

spec Alpha {
  fn alpha_inner() forall {
    assert(one() == 1);
  }
}

spec Beta {
  struct Counter {
    value: i32;
    fn next(self) -> i32 {
      return self.value + 1;
    }
  }
}

spec Gamma {
}
