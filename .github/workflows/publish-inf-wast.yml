name: Publish inf-wast crate

on:
  push:
    tags:
      - 'inf-wast-v*.*.*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
jobs:
  publish:
    name: Publish inf-wast to crates.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Verify package can be built
        run: cargo build --package inf-wast --release

      - name: Run tests
        run: cargo test --package inf-wast

      - name: Publish to crates.io
        run: cargo publish --package inf-wast --token ${{ secrets.CRATES_IO_TOKEN }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          name: inf-wast ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
