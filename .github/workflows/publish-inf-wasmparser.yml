name: Publish inf-wasmparser crate

on:
  push:
    tags:
      - 'inf-wasmparser-v*.*.*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
jobs:
  publish:
    name: Publish inf-wasmparser to crates.io
    runs-on: [self-hosted, X64, Linux]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Verify package can be built
        run: cargo build --package inf-wasmparser --release

      - name: Run tests
        run: cargo test --package inf-wasmparser

      - name: Publish to crates.io
        run: cargo publish --package inf-wasmparser --token ${{ secrets.CRATES_IO_TOKEN }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          name: inf-wasmparser ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
