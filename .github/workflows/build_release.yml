name: Build Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      release-build: true
      package-artifacts: true
      upload-to-release: true
